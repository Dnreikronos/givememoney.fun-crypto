# Deployer wallet private key (must start with 0x for Forge)
PRIVATE_KEY=0x...your_64_hex_chars

# Alchemy Sepolia RPC (create app at https://dashboard.alchemy.com → get API key)
# Format: https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY
RPC_URL=https://eth-sepolia.g.alchemy.com/v2/your_api_key_here

# Deployed StreamerDonations contract address (set after running deploy-testnet)
CONTRACT_ADDRESS=0x...deployed_contract_address

# Streamer's private key (used by register-streamer-testnet to call registerStreamer())
STREAMER_PRIVATE_KEY=0x...streamer_64_hex_chars

# Registered streamer address to send the test donation to
STREAMER_ADDRESS=0x...streamer_address
